The Upgrade task compares the current version of the cron with the target
version and then executes the necessary commands to bring the database up to
date

Available commands:
	app:cron          - Runs app:cron:upgrade
	app:cron --help   - Displays this help text
	app:cron:generate - Generates new cron upgrade template
	app:cron:upgrade  - Updates cron to newest version

Cron templates need to return an with the following structure:

$new_lines = array
(
	'group' => array
	(
		'task-name-no-spaces' => array
		(
			'command'     => '* * * * * echo "new cron command"',
			'description' => 'Optional description',
		),
	),
);

To remove a line from the cron, generate a new template and return the following array
$lines_to_remove = array
(
	'group' => array
	(
		'task-to-remove' => NULL,
	),
);

Future versions will implement downgrade and incremental step functionality.

Author: Andrew Reida <andrew@synapsestudios.com>
